{
  "name": "wait-for-vercel-deployment",
  "version": "0.1.0",
  "description": "Action that will wait for a specific vercel deployment to finish",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "1.6.0",
    "@actions/github": "5.0.0"
  }
}
